package terminalPortuaria;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class DryTest {
/*
    @Test
    void constructor_acepta_billgroup_y_rechaza_billregular() {
        BillGroup grupo = new BillGroup(new java.util.ArrayList<>());
        Dry dry = new Dry("D1", 10, 100, grupo);
        assertEquals("D1", dry.getIdentificador());

        // BillRegular should be rejected
        try {
            new Dry("D2", 10, 100, new BillRegular("X", 10));
            fail("Se esperaba IllegalArgumentException para BillRegular");
        } catch (IllegalArgumentException e) {
            // OK
        }
    }*/
}

